## Status Updates

Order status updates are role-restricted.

For MVP:
- Role checks are enforced directly in controller
- Status sequence is validated separately

This will be refactored into centralized permissions if needed.
